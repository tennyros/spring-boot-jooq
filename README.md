To start the application you need to specify VM options (for example: -Dspring.profiles.active=dev) and your environment variables (like: DB_BASE=postgresql;DB_NAME=your_db_name;DB_PASSWORD=your_password;DB_PORT=xxxx;DB_SERVER=localhost;DB_USERNAME=your_username) in run/debug configurations in Intellij.
